spring:
  application:
    name: eureka-server
  security:
    basic:
      enabled: true
    user:
      name: curso-eureka-user
      password: curso-eureka-pass

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false